# Feedback aggregation policy 1.0.0

This policy reserves the routing semantics for `feedback-decision/1.0.0` but is
intentionally inactive. Its thresholds remain `null` until the 300 development,
80 calibration, and 100 locked-test records are independently reviewed, including
120 double-annotated records.
